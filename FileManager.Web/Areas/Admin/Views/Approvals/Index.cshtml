@model IEnumerable<FileManager.Model.History>

@{
     ViewBag.Title = "Approvals";
}


<div id="listHeader">
     <div class="container-fluid">
          @using (Html.BeginForm("Index", "Approvals", FormMethod.Get, new { @class = "container-fluid" }))
          {
               <div class="col-lg-2 col-md-2 col-sm-2 col-xs-12">
                    <h2 class="text-success">
                         <span class="fa fa-download"></span> Approvals
                         <label class="badge">@Model.Count()</label>
                    </h2>
                    <p>records approval</p>
               </div>

               <div class="col-lg-2 col-md-2 col-sm-2 col-xs-12">

               </div>
               <div class="col-lg-2 col-md-2 col-sm-2 col-xs-12">


               </div>

               <div class="col-lg-3 col-md-3 col-sm-3 col-xs-12">

               </div>

               <div class="col-lg-3 col-md-3 col-sm-3 col-xs-12">

               </div>
          }
     </div>
</div>



<div id="listCon">
     <div class="container-fluid">
          <table class="table table-condensed table-hover table-striped table-bordered table-responsive">
               <thead>
                    <tr>
                         <th>
                              S/N
                         </th>
                         <th>
                              Requested By
                         </th>
                         <th>
                              File Name
                         </th>
                         <th>
                              Date Submitted
                         </th>
                    </tr>
               </thead>
               <tbody>
                    @foreach (var item in Model.OrderByDescending(e => e.Date))
               {
                         <tr>
                              <td>
                                   <a href="/Approvals/Approve/@item.Id"></a>
                              </td>
                              <td>
                                   <strong>
                                        @item.AppUser.Firstname @item.AppUser.Lastname
                                   </strong>
                              </td>
                              <td>
                                   @item.File.Name
                              </td>
                              <td>
                                   @item.Date.ToLongDateString()
                              </td>
                         </tr>
                    }
               </tbody>



          </table>
     </div>
</div>